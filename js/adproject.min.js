// Copyright © A Digital Project 2019
var secret = 0;

if (secret === 0){
	$(document).keydown(function(keyPressed) {
		if (keyPressed.keyCode === 38) {
			$(document).keydown(function(keyPressed) {
				if (keyPressed.keyCode === 38) {
					$(document).keydown(function(keyPressed) {
						if (keyPressed.keyCode === 40) {
							$(document).keydown(function(keyPressed) {
								if (keyPressed.keyCode === 40) {
									$(document).keydown(function(keyPressed) {
										if (keyPressed.keyCode === 37) {
											$(document).keydown(function(keyPressed) {
												if (keyPressed.keyCode === 39) {
													$(document).keydown(function(keyPressed) {
														if (keyPressed.keyCode === 37) {
															$(document).keydown(function(keyPressed) {
																if (keyPressed.keyCode === 39) {
																	$(document).keydown(function(keyPressed) {
																		if (keyPressed.keyCode === 66) {
																			$(document).keydown(function(keyPressed) {
																				if (keyPressed.keyCode === 65 && secret == 0) {
																					alert("test");
																					secret = 1;
																				}
																			});
																		}
																	});
																}
															});
														}
													});
												}
											});
										}
									});
								}
							});
						}
					});
				}
			});
		}
	});
}

var devtools = /./;
devtools.toString = function() {
	this.opened = true;
}

console.log("%c Have you ever tried typing the Konami Code?", "background: black; color: white; font-size: 24px;");

function Show(object) {
	object.style.width = "auto";
	object.style.height = "auto";
	object.children[0].style.display = "inline-block";
}

function Hide(object) {
	object.style.width = "";
	object.style.height = "";
	object.children[0].style.display = "";
}
